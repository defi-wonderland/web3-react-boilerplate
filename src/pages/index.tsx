import { Landing } from '~/containers';
import { AppLayout } from '~/containers';

const HomePage = () => {
  return (
    <AppLayout>
      <Landing />
    </AppLayout>
  );
};

export default HomePage;
